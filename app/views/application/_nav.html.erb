<nav class="bg-blue-600 text-white shadow-lg mb-6">
  <div class="container mx-auto px-4">
    <div class="flex justify-between items-center py-4">
      <%= link_to 'Library', root_path, class: "text-xl font-bold" %>

      <div class="hidden md:flex space-x-6">
        <%= link_to 'Books', books_path, class: "hover:text-blue-200" %>
        <% if current_user&.librarian? %>
          <%= link_to 'Borrowings', borrowings_path, class: "hover:text-blue-200" %>
        <% end %>
        <%= link_to 'Dashboard', dashboard_books_path, class: "hover:text-blue-200" %>
      </div>

      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <span class="text-sm"><%= current_user.email %></span>
          <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "bg-blue-700 hover:bg-blue-800 px-3 py-1 rounded" %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: "hover:text-blue-200" %>
          <%= link_to 'Sign Up', new_user_registration_path, class: "bg-blue-700 hover:bg-blue-800 px-3 py-1 rounded" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>